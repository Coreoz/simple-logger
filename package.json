{
  "name": "simple-logger",
  "version": "1.0.0",
  "description": "A simple JS Logger written in TypeScript",
  "author": "Aurélien Manteaux <amanteaux@coreoz.com> (https://coreoz.com)",
  "repository": "https://github.com/coreoz/simple-logger",
  "homepage": "https://github.com/coreoz/simple-logger",
  "license": "Apache-2.0",
  "keywords": ["logger", "logback", "slf4j"],
  "scripts": {
    "build": "npm run tsc:cjs && npm run tsc:esm",
    "tsc:cjs": "tsc --module commonjs --outDir build/cjs -p tsconfig.json",
    "tsc:esm": "tsc --module esnext --outDir build/esm -p tsconfig.json",
    "clean": "rm -rf build",
    "lint": "tsc --noEmit -p tsconfig.json && eslint src --ext .ts",
    "prepare-release": "npm run clean && npm run lint && npm run build",
    "publish-patch": "npm run prepare-release && npm version patch && git push && npm publish",
    "publish-minor": "npm run prepare-release && npm version minor && git push && npm publish",
    "publish-major": "npm run prepare-release && npm version major && git push && npm publish"
  },
  "main": "build/cjs/index.js",
  "typings": "build/esm/index.d.ts",
  "module": "build/esm/index.js",
  "engines": {
    "node": ">=10"
  },
  "dependencies": {},
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.0.1",
    "@typescript-eslint/parser": "^4.0.1",
    "eslint": "^7.8.0",
    "eslint-config-airbnb-typescript": "^12.0.0",
    "eslint-plugin-import": "^2.22.0",
    "eslint-plugin-jsx-a11y": "^6.4.1",
    "eslint-plugin-react": "^7.21.5",
    "ts-node": "^9.0.0",
    "typedoc": "^0.19.0",
    "typescript": "^4.0.2"
  },
  "files": [
    "build/**/*.*"
  ]
}
